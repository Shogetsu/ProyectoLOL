import { CampeonesFiltrorolPipe } from './campeones-filtrorol.pipe';

describe('CampeonesFiltrorolPipe', () => {
  it('create an instance', () => {
    const pipe = new CampeonesFiltrorolPipe();
    expect(pipe).toBeTruthy();
  });
});
