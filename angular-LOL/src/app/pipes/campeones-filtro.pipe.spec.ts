import { CampeonesFiltroPipe } from './campeones-filtro.pipe';

describe('CampeonesFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new CampeonesFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
